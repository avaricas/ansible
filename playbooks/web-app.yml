---
- name: Configure Web Server.
  hosts: localhost
  connection: local
  gather_facts: true
  become: true
  vars:
    tools:
      extras: true
      packages: true
      timezone: true
      bbr: true

  tasks:
    - name: Import | Extras role.
      ansible.builtin.import_role:
        name: extras
    - name: Import | NGINX Role.
      ansible.builtin.import_role:
        name: nginx

